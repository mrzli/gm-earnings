import React from 'react';

interface SalaryBreakdownInputProps {}

export function SalaryBreakdownInput(
  props: SalaryBreakdownInputProps
): React.ReactElement {
  return <div>{'SalaryBreakdownInput'}</div>;
}
